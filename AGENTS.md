# AGENTS

Global memory for Droid CLI.

## Project
- Name: redcode IM
- Type: Multi-module chat application
- Modules: `frontend`, `backend`, `admin`

## Backend
- Language/Framework: Rust + Axum
- Endpoints: `GET /`, `GET /healthz`, `GET /ws` (WebSocket echo)
- Port: `8080` (override with `PORT`)

## Environment
- Local toolchain: Rust/cargo not yet installed on host

## Notes
- Update this file with key decisions and shared context as the project evolves.
