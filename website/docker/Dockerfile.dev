# 开发环境 Dockerfile
FROM oven/bun:1.3.1-alpine

WORKDIR /app

# 安装必要的工具
RUN apk add --no-cache git wget

# 复制依赖文件
COPY package.json bun.lock ./

# 安装依赖
RUN bun install

# 复制源代码（开发时使用 volume 挂载，这里仅作为默认）
COPY . .

# 暴露端口
EXPOSE 8015

# 启动开发服务器
CMD ["bun", "run", "dev"]

