# PRD-005: Backend Rust API 测试覆盖率提升 - Phase 5

> 版本: 1.0
> 日期: 2026-01-22
> 目标: 版本管理与热更新 API 测试

## 1. 背景

Phase 4 完成后，Rust API 测试达到 141 个用例。本阶段聚焦版本管理与热更新 API。

### 1.1 当前状态

| 指标 | 数值 |
|------|------|
| 测试模块 | 16 |
| 测试用例 | 141 |

### 1.2 Phase 5 目标模块

| Handler | 大小 | 路由数 | 说明 |
|---------|------|--------|------|
| version.rs | 1360 行 | 10+ | 应用版本管理 |
| (热更新) | - | 5 | 热更新管理 |

## 2. 路由分析

### 2.1 应用版本管理

```
GET    /api/admin/app-versions                    # 版本列表
POST   /api/admin/app-versions                    # 创建版本
GET    /api/admin/app-versions/{id}               # 版本详情
DELETE /api/admin/app-versions/{id}               # 删除版本
POST   /api/admin/app-versions/{id}/deactivate    # 停用版本
POST   /api/admin/app-versions/upload/signature   # 上传签名
```

### 2.2 热更新管理

```
GET    /api/admin/hot-updates                     # 热更新列表
GET    /api/admin/hot-updates/events              # 热更新事件
GET    /api/admin/hot-updates/{id}                # 热更新详情
POST   /api/admin/hot-updates/{id}/activate       # 激活热更新
POST   /api/admin/hot-updates/{id}/deactivate     # 停用热更新
```

## 3. 测试用例设计

### 3.1 version_tests.rs (预计 12 用例)

| 测试用例 | 描述 |
|----------|------|
| `admin_list_app_versions_success` | 获取版本列表 |
| `admin_list_app_versions_requires_admin` | 需要管理员权限 |
| `admin_create_app_version_success` | 创建新版本 |
| `admin_create_app_version_missing_fields` | 缺少必填字段 |
| `admin_get_app_version_success` | 获取版本详情 |
| `admin_get_app_version_not_found` | 版本不存在 |
| `admin_delete_app_version_success` | 删除版本 |
| `admin_delete_app_version_not_found` | 删除不存在版本 |
| `admin_deactivate_app_version_success` | 停用版本 |
| `admin_get_upload_signature_success` | 获取上传签名 |
| `admin_list_hot_updates_success` | 获取热更新列表 |
| `admin_get_hot_update_events_success` | 获取热更新事件 |

### 3.2 预期结果

- 测试用例: 141 → **153+**
- 增量: +12 用例 (+8.5%)

## 4. 验收标准

1. 所有新增测试通过 `cargo test --test api`
2. 无回归失败
3. 测试代码遵循现有模式
4. 输出 TEST-005 测试报告
