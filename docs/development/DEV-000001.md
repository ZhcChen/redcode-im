# 开发记录：devflow 架构文档全量迁移

## 1. 目标与范围

- 将 devflow-core 工作流文档与技能集引入本仓库
- 统一 `docs/` 结构，迁移原业务文档到 `docs/reference/`
- 建立单一入口 `docs/index.md`

## 2. 关联文档

- PRD：无（流程迁移任务）
- SPEC：`docs/specs/SPEC-000001.md`

## 3. 实现映射（验收矩阵 → 实现）

| 验收项/用例 | 模块/接口 | 说明 |
|------------|----------|------|
| 统一文档入口 | docs/index.md | 入口与索引合并 | 
| 引入 devflow 标准目录 | docs/workflow + docs/templates + docs/conventions | 流程与模板落盘 |
| 迁移业务文档 | docs/reference/** | 原 api/architecture/... 迁移 | 
| 技能落盘 | skills/devflow-* | devflow-core skills 复制并排除 .env |
| 更新引用 | 全仓库文档链接 | 替换到新路径 |

## 4. 实现清单

- 迁移 `docs/api`、`docs/architecture`、`docs/guides`、`docs/operations`、`docs/security`、`docs/legal`、`docs/testing` 至 `docs/reference/`
- 引入 devflow-core 文档目录（workflow/templates/conventions/requirements/...）
- 引入 devflow-core skills（排除 `.env`）
- 重写 `docs/index.md` 作为统一入口
- 更新全仓库文档引用路径

## 5. 关键实现说明

- 保持 `docs/reports/` 路径不变，避免 Dashboard 读取路径改变
- `docs/index.md` 同时承载流程入口与业务文档索引
- 不引入 devflow-core 示例编号文件，避免与现有编号冲突

## 6. 变更列表

- 新增：`docs/workflow/**`、`docs/templates/**`、`docs/conventions/**`、`docs/requirements/**`、`docs/development/**`、`docs/reviews/**`、`docs/tests/**`、`docs/backlog/**`、`skills/devflow-*`
- 修改：`docs/index.md`、`AGENTS.md`、全仓库文档引用
- 删除：无

## 7. 自测清单（对应验收矩阵）

| 用例ID | 验收项 | 结果 | 备注 |
|--------|--------|------|------|
| TC-000001-001 | 入口文档可访问 | 通过 | `docs/index.md` 更新完成 |
| TC-000001-002 | 参考文档链接可达 | 通过 | 主要路径已替换 |
| TC-000001-003 | reports 路径保持 | 通过 | 未迁移 `docs/reports/` |

## 8. 影响评估（可选）

- 数据库：无
- 配置：无
- 兼容性：文档路径变更，需依赖更新引用
- 依赖：新增 `skills/devflow-*`

## 9. 风险与回滚（可选）

- 风险：遗漏链接更新导致跳转失败
- 回滚：恢复旧目录结构与 `docs/index.md`，移除 `docs/reference/` 与新增目录

## 10. 待办与后续

- 补充后续同步 devflow-core 的机制（subtree 或脚本）
- 复查所有文档链接（边角路径）
